/** @type {import('next').NextConfig} */
const nextConfig = {
  // reactStrictMode: true,
  // // ssr, displayName 이 기본값 true로 켜진다.
  // styledComponents: true,
  // images: {
  //   domains: ["cmd8-aight.s3.amazonaws.com"],
  // },
};

module.exports = nextConfig;
